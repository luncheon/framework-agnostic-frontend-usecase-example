{
  "name": "usecase",
  "version": "0.0.0",
  "module": "es/index.js",
  "types": "es/index.d.ts",
  "private": true,
  "license": "WTFPL",
  "scripts": {
    "build": "npm run clean && npm run lint && npm test && tsc -p .",
    "watch": "npm run clean && tsc -p . -w",
    "lint": "eslint src/**/*.ts",
    "clean": "rimraf es",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@typescript-eslint/eslint-plugin": "^1.9.0",
    "eslint": "^5.16.0",
    "immer": "^3.1.1",
    "jest": "^24.8.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.5"
  }
}
